name: Run changed Ansible roles

on:
  push:
    paths:
      - 'ansible/roles/**'

jobs:

  detect:
    runs-on: docker
    container:
      image: git.dbnet.work/dborshi/ansible-runner:latest
    outputs:
      changed_roles: ${{ steps.diff.outputs.roles }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get changed roles as JSON array
        id: diff
        run: |
          ROLES=$(git diff --name-only HEAD~1 HEAD \
            | grep '^ansible/roles/' \
            | awk -F'/' '{print $3}' \
            | sort -u | jq -R . | jq -sc .)
          echo "roles=$ROLES" >> $GITHUB_OUTPUT
          echo "Detected: $ROLES"

  deploy:
    needs: detect
    if: needs.detect.outputs.changed_roles != '[]'
    runs-on: docker
    container:
      image: git.dbnet.work/dborshi/ansible-runner:latest
    strategy:
      matrix:
        role: ${{ fromJson(needs.detect.outputs.changed_roles) }}
      fail-fast: false

    steps:
      - uses: actions/checkout@v4

      - name: Start SSH agent
        run: |
          eval "$(ssh-agent -s)"
          echo "${{ secrets.SSH_PRIVATE_KEY }}" | tr -d '\r' | ssh-add -

      - name: Read target hosts from role meta
        id: hosts
        run: |
          META="ansible/roles/${{ matrix.role }}/meta/hosts.yml"

          if [ ! -f "$META" ]; then
            echo "ERROR: missing meta file $META"
            exit 1
          fi

          TARGET=$(awk -F': ' '/target_hosts/ {print $2}' "$META")

          if [ -z "$TARGET" ]; then
            echo "ERROR: target_hosts is empty for role ${{ matrix.role }}"
            exit 1
          fi

          echo "target=$TARGET" >> $GITHUB_OUTPUT

      - name: Run role
        run: |
          cat > /tmp/playbook.yml <<EOF
          ---
          - name: Apply ${{ matrix.role }}
            hosts: ${{ steps.hosts.outputs.target }}
            become: true
            roles:
              - role: ${{ matrix.role }}
          EOF
          ansible-playbook /tmp/playbook.yml \
            -i ansible/inventory/hosts.ini \
            --roles-path /workspace/dborshi/proxmox-iac/ansible/roles